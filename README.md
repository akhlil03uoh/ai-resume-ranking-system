AI Resume Ranking & Parsing System

This project parses multiple resumes,
extracts skills, compares with job description,
and ranks candidates using scoring algorithm.

Features:
- Resume upload
- Resume parsing
- Skill extraction
- JD matching
- Candidate ranking
- Dashboard

Run:

pip install -r requirements.txt
streamlit run app.py
